# Server Configuration
PORT=5000
NODE_ENV=development

# Google Cloud Configuration (REQUIRED for hackathon)
GOOGLE_PROJECT_ID=your-google-cloud-project-id
GOOGLE_APPLICATION_CREDENTIALS=./config/service-account-key.json

# Client Configuration
CLIENT_URL=http://localhost:3000

# Optional: Database Configuration
MONGODB_URI=mongodb://localhost:27017/craftconnect

# Optional: Additional API Keys (if needed)
# WHATSAPP_API_TOKEN=your-whatsapp-token
# OPENAI_API_KEY=your-openai-key

# Rate Limiting (optional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Security (optional - for production)
JWT_SECRET=your-jwt-secret-key-here
SESSION_SECRET=your-session-secret-here

# Google Cloud Service Account Instructions:
# 1. Go to Google Cloud Console
# 2. Create a new project or select existing one
# 3. Enable Speech-to-Text API and Vertex AI API
# 4. Create a service account and download JSON key
# 5. Place the JSON file in ./config/service-account-key.json
# 6. Set GOOGLE_PROJECT_ID to your project ID from the JSON file